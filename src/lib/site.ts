/**
 * Site-wide constants.
 *
 * The contact address is here rather than inline because it appeared verbatim in
 * four pages, which is exactly how a stale address survives a rename.
 */
export const CONTACT_EMAIL = "lixd220@gmail.com";
